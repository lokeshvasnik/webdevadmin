import AddProjectForm from "./AddProjectForm";

const Projects = () => {
  return (
    <div>
      <AddProjectForm />
    </div>
  );
};

export default Projects;

/*

Form to add all the project details including images

On the other side i can show all the projects which are available

*/
